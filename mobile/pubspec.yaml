name: focus_day
description: MVP FocusDay - Goal management with persistence.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  intl: ^0.18.1
  provider: ^6.0.5
  table_calendar: ^3.0.9
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^17.0.0
  workmanager: ^0.5.2
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
